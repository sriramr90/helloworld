{
  "name": "goodvibe",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A daily morning-newspaper PWA of genuinely positive news from around the world.",
  "scripts": {
    "build": "node scripts/build-edition.mjs",
    "serve": "node scripts/serve.mjs",
    "dev": "node scripts/build-edition.mjs && node scripts/serve.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.69.0"
  }
}
